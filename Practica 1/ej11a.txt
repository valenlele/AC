ORG 1000H
A DB 10
B DB 5
RES DW ?

ORG 3000H
DIV: PUSH AX
PUSH BX
PUSH CX

MOV BX, SP
ADD BX, 10
MOV AL, [BX]
ADD BX, 1
MOV AH, [BX]

CMP AL, 00h
JZ IND

MOV CX, 0
LOOP: CMP AH, AL
JC GUARDAR
SUB AH, AL
INC CX
JMP LOOP

IND: MOV BX, SP
ADD BX, 8
MOV WORD PTR [BX], 7FFFh
JMP FIN

GUARDAR: SUB BX, 3
MOV [BX], CX

FIN: POP CX
POP BX
POP AX
RET

ORG 2000H
MOV AH, A
MOV AL, B
PUSH AX

MOV AX, RES
PUSH AX

CALL DIV
POP CX
MOV RES, CX

POP AX
HLT
END